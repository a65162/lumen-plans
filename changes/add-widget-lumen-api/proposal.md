## Why

在 lumen-api 落地產品規格 `add-widget` 的 widget JSON。

## What Changes

- 種子 widget JSON 含 `description`
- `GET /widgets` 的元素含字串欄位 `description`

## Capabilities

### New Capabilities

- （無。本 repo 只落地產品規格 `widget` JSON，沒有獨立可觀察行為。）

### Modified Capabilities

- （無。`skip_specs: true`）

## Impact

- 僅 lumen-api
- 產品規格變更：`add-widget`
